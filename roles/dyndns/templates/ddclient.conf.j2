# Configuration file for ddclient generated by debconf
#
# /etc/ddclient.conf
#
# was 6 lines; all on one line by HL

protocol={{ dyndns_protocol }}, \
use=if, if=eth0, \
server={{ dyndns_server }}, \
login={{ dyndns_login }}, \
password='{{ dyndns_password }}', \
@
